name: Mirror to hello-sparkle-world-31
on:
  push:
    branches:
      - main
permissions:
  contents: read
jobs:
  mirror:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
          persist-credentials: false # CRITICAL: Prevents default token override

      - name: Push mirror
        env:
          MIRROR_TOKEN: ${{ secrets.MIRROR_TOKEN }}
        run: |
          git remote add mirror https://x-access-token:${MIRROR_TOKEN}@github.com/Earsongchay/hello-sparkle-world-31.git
          git push mirror main --force
